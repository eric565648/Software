<launch>
	<arg name="pkg_name" value="me212arm"/>
	<arg name="node_name" default="joy_points_arm_controller"/>
		
	<arg name="veh" doc="Name of vehicle. ex: megaman"/>
	<arg name="local" default="false" doc="true for running on laptop. false for running on vehicle."/>
	<arg name="config" default="baseline" doc="Specify a config."/>
	<arg name="param_file_name" default="default" doc="Specify a param file. ex:megaman"/>

	<include file="$(find duckietown)/machines"/>
	<!-- Joy node -->
	<node ns="$(arg veh)" if="$(arg local)" pkg="joy" type="joy_node" name="joy" output="screen">
        <rosparam command="load" file="$(find duckietown)/config/$(arg config)/joy/joy_node/$(arg param_file_name).yaml"/>
    </node>
    <node ns="$(arg veh)" unless="$(arg local)" machine="$(arg veh)" pkg="joy" type="joy_node" name="joy" output="screen">
        <rosparam command="load" file="$(find duckietown)/config/$(arg config)/joy/joy_node/$(arg param_file_name).yaml"/>
    </node>

	<!-- Run on local (laptop) -->
	<node if="$(arg local)" ns="$(arg veh)" pkg="$(arg pkg_name)" type="joy_mapper_target_point.py" name="joy_mapper_target_poin" output="screen" clear_params="true" required="true">
		<rosparam command="load" file="$(find duckietown)/config/$(arg config)/joy_mapper/joy_mapper_node/$(arg param_file_name).yaml"/>
	</node>

	<!-- Run on remote (vehicle) -->
	<include unless="$(arg local)" file="$(find duckietown)/machines"/>
	<node unless="$(arg local)" ns="$(arg veh)" machine="$(arg veh)" pkg="$(arg pkg_name)" type="joy_mapper_target_point.py" name="joy_mapper_target_point" output="screen" clear_params="true" required="true">
		<rosparam command="load" file="$(find duckietown)/config/$(arg config)/joy_mapper/joy_mapper_node/$(arg param_file_name).yaml"/>
	</node>	

	<!--Run points to joints-->
	<node pkg="$(arg pkg_name)" type="point_to_joints.py" name="point_to_joints" output="screen">
	</node>

	<!-- Subscribe Topics -->
	<!-- "joy": sensor_msgs/Joy. The command read from joystick -->
	<!-- Publish Topics -->
	<!-- "~wheels_cmd": duckietown_msgs/WheelsCmd. Wheels command for duckiebot -->
</launch>
